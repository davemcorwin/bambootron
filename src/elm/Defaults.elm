module Defaults exposing (..)

import Grid exposing (Grid)


defaults : Grid
defaults =
    { numCols = 100
    , numRows = 100
    , colWidth = 100
    , rowHeight = 35
    , colHeaderWidth = 50
    , gridGap = 1
    }
