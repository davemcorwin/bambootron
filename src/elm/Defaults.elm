module Defaults exposing (..)

import Grid exposing (Grid)


defaults : Grid
defaults =
    { numCols = 26
    , numRows = 50
    , colWidth = 100
    , rowHeight = 35
    , colHeaderWidth = 50
    , gridGap = 1
    }
